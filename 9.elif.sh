#!/bin/bash

x=3

if [[ $x > 7 ]]; then
	echo "x მეტია შვიდზე და მისი მნიშვნელობა არის: $x"
elif [[ $x <= 5 ]]; then # "elif" როგორც ახალი დებულების ცდა
	echo "x ნაკლებია ხუთზე ან ტოლია ხუთის და მისი მნიშვნელობა არის: $x"
elif [[ $x == 15 ]]; then
	echo "x ტოლია თხუთმეტის და მისი მნიშვნელობა არის: $x"
else
	echo "როცა დებულება არასწორია"
fi
